{% extends 'base.html.twig' %}

{% block title %}Hello ServicesController!{% endblock %}

{% block stylesheets %}
    <style>
        .card img {
            min-height: 25rem;
            max-height: 25rem;
          }
    </style>
{% endblock %}

{% block body %}
    <h1 class="text-center text-warning">Services</h1>

    <div class="card-deck container-fluid">
        <div class="row">

            {% for service in services %}
                <div class="card col-12 col-md-4 bg-transparent text-light text-center border-warning">
                    <img src="{{ asset('Pictures/upload/service/') ~ service.picture }}" class="card-img-top img-thumbnail mt-2" alt="{{ service.title }}">
                    <div class="card-body">
                        <h5 class="card-title text-uppercase text-warning">{{ service.title }}</h5>
                        <p class="card-text ">{{ service.content|raw }}</p> 
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
